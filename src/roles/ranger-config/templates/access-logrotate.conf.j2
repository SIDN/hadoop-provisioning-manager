# Created on {{ ansible_date_time.iso8601 }}
# Pushed to: {{  inventory_hostname }}

# Logrotate schedule for the embedded Tomcat server running inside Ranger process.
# this cannot be done with log4j cfg.

{{ ranger_log_dir }}/access-*.log {
    daily
    compress
    dateext
    rotate 3
    missingok
}