# Created on {{ ansible_date_time.iso8601 }}
# Pushed to: {{  inventory_hostname }}

[Unit]
Description=Ranger
After=network.target
StartLimitIntervalSec=0

[Service]
Type=forking
User=ranger
Group=ranger
ExecStart={{ stack_root }}/apache-ranger-usersync-latest/ranger-usersync-services.sh start
ExecStop={{ stack_root }}/apache-ranger-usersync-latest/ranger-usersync-services.sh stop
Restart=always
RestartSec=60

[Install]
WantedBy=multi-user.target