# Created on {{ ansible_date_time.iso8601 }}
# Pushed to: {{  inventory_hostname }}

yarn.nodemanager.linux-container-executor.group={{ hadoop_os_group }}
min.user.id=100
allowed.system.users=nobody,{{ spark_os_user }},{{ impala_os_user }},{{ hive_os_user }},{{ hadoop_os_user.yarn }}
banned.users=mapred,bin
feature.terminal.enabled=1

